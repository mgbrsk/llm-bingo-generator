# (Work in process) LLM генератор бинго

## Описание

Создаёт png картинки заданного размера и заполняет текстом на заданную тему. Требует ллм (локальную или удалённую), доступную requests с методами /v1/completions и /v1/chat/completions (например поднятую через через LM Studio)

## Пример использования

```python
python main.py --cell_card_amount=4 --texts="Архитектура марса"
```

## Python version

3.12

## Текущий результат

Иногда что-то генерируется, иногда нет. Пока качество результата низкое, но результат забавный.

## Чего дальше

- попробовать улучшить промты
- попробовать другие модельки
- добавить телеграм чат бот
- галерею в чат боте
